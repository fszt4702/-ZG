# 智肝 · 开发日志

> 版本：v3.0  
> 周期：2026年7月28日 — 2026年8月20日（约3周半）  
> 项目：软硬件协同的肝脏疾病居家智能检测与慢病全病程管理平台

---

## 第一阶段：项目骨架搭建（7月28日）

### 项目初始化
- 创建微信小程序项目，配置 `app.json`：注册全部7个页面路由、3Tab导航栏（首页 / 我的报告 / 健康趋势）
- 配置 `project.config.json`，启用 Canvas 2D、ES6转译、PostCSS 等
- 编写全局公共样式 `app.wxss`：定义医疗蓝配色体系（主色 `#6C9BD8`、深色 `#3C5068`、灰色阶）、通用布局类（flex-row/column/between/center）、卡片 `.card`、按钮 `.btn-primary/outline/danger`、标签 `.tag` 等

### 组件开发
- 实现 `empty-state` 空状态占位组件：支持自定义图标、标题、描述、操作按钮，用于无数据场景

### 页面框架搭建
- **登录页** `login`：完成页面布局结构（Logo区 + 特性展示 + 登录入口 + 手机号表单 + 底部声明）
- **检测页** `detect`：三步流程骨架（拍照采样 → 分析中 → 完成），Canvas 2D 隐藏画布用于像素分析
- **报告页** `report`：报告详情页路由配置
- **家庭管理** `family`：成员列表 + 底部弹窗表单的布局

### 工具层
- 完成 `chart.js` Canvas 趋势图绘制工具：网格线、GCA/AFP参考线、数据折线、数据点标注、图例

### 遇到的问题
- 无

---

## 第二阶段：视觉与交互完善（7月29日）

### TabBar 图标
- 制作并导入 6 张 TabBar 图标（home / report / trend，各含普通态和选中态），采用 PNG 格式

### 首页模板
- 完成 `index.wxml`：用户信息头栏、首次使用引导卡片（无报告时显示）、2×2 快捷操作宫格、肝脏养护小贴士列表

### 遇到的问题
- 无

---

## 第三阶段：核心逻辑层搭建（7月30日）

### 登录模块
- 实现 `login.js`：微信 `wx.login` 一键登录 → `app.saveLoginState()` 保存全局状态
- 支持手机号快捷登录（+86号段校验、协议勾选）
- 登录失败自动降级为 Mock 用户信息
- 首次登录自动创建默认家庭成员（"本人"）
- 已登录用户自动跳转首页

### 应用入口
- 实现 `app.js`：`onLaunch` 初始化云开发环境 + 检查本地登录态
- `saveLoginState`：登录成功后保留已有成员数据，首次登录创建默认成员
- `logout`：清除全部本地存储

### 家庭成员管理
- 实现 `family.js`：成员列表加载、当前成员切换（写入 `globalData` + Storage）
- 底部弹窗表单：姓名/关系/性别/年龄 四项，关系选项支持9种，性别3种
- 编辑（默认成员不可编辑）、删除（默认成员不可删除），删除当前成员后自动切换

### 首页样式
- 完成 `index.wxss`：用户头像区、欢迎卡片、2×2宫格、小贴士列表全部样式

### 云函数搭建
- 创建 `kimi-proxy` 云函数目录结构：`index.js` + `package.json` + `config.json`（60s 超时配置）
- 云函数中配置 Kimi API Key（服务端保管，客户端不可见）

### 项目文档
- 编写 `README.md`：项目简介、MVP功能表、技术栈、目录结构、快速开始、功能流程图、API降级策略、数据结构、免责声明

### 遇到的问题
- 无

---

## 第四阶段：趋势页完善（8月3日）

### 趋势页面模板
- 完成 `trend.wxml`：趋势概览摘要卡片、Canvas 折线图区域（含 GCA/AFP 双色图例）、历史记录明细表格（日期/GCA值/AFP值/风险等级）、空状态提示

### 遇到的问题
- 无

---

## 第五阶段：核心引擎重构（8月10日）

### 规则引擎 `thresholds.js`（全新编写）
这是整个分析系统的核心。之前各模块各自判断风险等级，存在不一致、不统一的问题。

- **灰度 → 浓度映射**：定义 `GCA_THRESHOLDS` 和 `AFP_THRESHOLDS` 两张映射表，将条带灰度值（0-255）通过分段线性插值转换为 GCA 浓度（mg/L）和 AFP 浓度（ng/mL）
- **四级风险判定**：正常（灰度150-255）、轻度（100-150）、中度（50-100）、重度（0-50）
- **综合评级**：取 GCA 和 AFP 中风险更严重的一方作为最终等级
- **C线质控**：C线灰度 ≥ 50 判定试纸有效，否则返回 `isValid: false`
- **统一输出结构**：所有分析路径（AI/本地/Mock）统一输出格式
- **风险配置表**：统一管理阴性/阳性/弱阳性/强阳性的显示文案、颜色、图标、建议

### 全局样式精调
- 将原来的深蓝配色（`#1B365D` / `#2E86AB`）全面迁移为浅蓝极简风格（主色 `#6C9BD8`）
- 统一页面背景 `#F4F7FA`、卡片描边 `#E4EAF0`、文字色阶
- 新增标签颜色体系：正常绿 `#E6F4EC`、轻度橙 `#FFF5E8`、中度深橙 `#FFF0E0`、重度红 `#FFECEC`

### 报告归档页模板
- 完成 `archives.wxml`：筛选栏（全部/阴性/阳性）+ 计数、下拉筛选选项、时间线列表（含日期/风险标签/GCA-AFP双值/成员名/删除）、空状态

### 报告详情页（全面重写）
- 实现 `report.js`：根据 reportId 读取本地存储、兼容新旧数据格式、预览原始试纸图像（`wx.previewImage`）、分享（`wx.showShareMenu`）、删除确认
- 完成 `report.wxml`：报告头部（风险徽章 + 检测人/日期）、风险圆环、条带灰度分析三线卡片（C线/T1/T2，含灰度条+数值+状态）、兼容旧数据的指标卡片、AI建议区、分析来源标签、免责声明、原始图像链接、底部三按钮操作栏
- 完成 `report.wxss`：报告页全部样式——风险圆环、指标大数字、灰度进度条、来源标签等

### AI 接口封装 `kimi.js`（重构）
- **改变架构**：之前部分判定逻辑散落在云函数中，现统一为：云函数只返回原始条带灰度数据 → 本地 `thresholds.evaluate()` 统一计算浓度和风险等级
- `imageToBase64`：`wx.getFileSystemManager().readFile` 将本地图片转 base64
- `analyzeTestStrip`：调用云函数 `kimi-proxy` → 获取 T1/T2/C 线灰度 → `thresholds.evaluate()` 输出完整结果
- `getMockResult`：4种模拟场景（正常/轻度/中度/重度）随机选一 → 统一走规则引擎

### 云函数重写
- 重写 `kimi-proxy/index.js`：使用 `https.request` 直连 Kimi API
- System prompt 精调：明确 C线/T1/T2 物理位置（上中下），要求返回纯 JSON
- Prompt 中给出灰度参考标尺（纯白=255, 极浅=200, 中等=128, 较深=60, 纯黑=0）
- response 解析兼容 markdown 代码块包裹（`jsonStr.replace(/^```json?\s*/i, '').replace(/\s*```$/i, '')`）
- 返回数据仅包含原始灰度值 + 有效性标记，不再做业务判定

### 遇到的问题
1. **规则引擎分散问题**：之前 GCA/AFP 判定逻辑在 AI、本地分析、Mock 三处各有实现，维护困难且结果不一致 → 统一抽取 `thresholds.js` 作为单一数据源
2. **配色风格不统一**：部分页面还残留旧版深蓝配色 → 全局重构为浅蓝极简风格，`app.wxss` 统一定义

---

## 第六阶段：全链路打通与最终打磨（8月11日凌晨）

### 本地分析引擎 `analyzer.js`（全新编写）
- `sampleRegion`：从 Canvas 上下文中采样指定矩形区域（百分比定位）的平均灰度，加权公式 `0.299R + 0.587G + 0.114B`
- `validateStripImage`：**图像有效性校验**——将图像水平分割为10条横带，分别采样中心区域灰度，基于 4 个条件判断是否为有效试纸照片：
  1. 平均灰度 ≥ 130（不能整体偏暗）
  2. 亮带（灰度 > 150）≥ 7条（大面积白色底板）
  3. 暗带（灰度 < 80）≥ 1条（至少有一条显色带）
  4. 暗带（灰度 < 80）≤ 3条（不能是杂乱图片）
- 条带区域配置 `BAND_REGIONS`：C线（y:0.22）、T1（y:0.40）、T2（y:0.55），各占高8-10%、宽50%

### 存储层 `storage.js`（重构完善）
- 用户信息 / 家庭成员 / 当前成员 / 检测报告 四大模块 CRUD
- 报告按 memberId 筛选、按日期排序（趋势图用 `localeCompare`）
- `initMockData`：首次使用时生成 6 组模拟历史数据（逐次加重：正常→正常偏高→轻度→轻中度→中度→重度），每组间隔14天，全部走规则引擎生成

### 工具函数 `util.js`（完善）
- 日期格式化、风险信息查询（统一引用 `thresholds.RISK_CONFIG`）、Toast/Modal 封装

### 检测页 `detect.js`（核心模块全面实现）
这是整个小程序的**核心页面**，实现了三层降级分析流水线：

1. **图像校验** → `analyzer.validateStripImage()`，不通过则显示具体原因和拍照指导
2. **AI 分析**（第一优先）→ 15s 超时，失败则
3. **本地 Canvas 分析**（第二优先）→ 10s 超时，失败则  
4. **Mock 兜底** → `kimi.getMockResult()`

- 进度动画：阶段1步骤动画（5步，每步1s）→ 阶段2缓推（90%→99%）
- Canvas 图片加载：`canvas.createImage()` + `drawImage` 等比缩放居中
- 结果保存：统一 `storage.saveReport()`，条带灰度+浓度值+风险判定+元信息
- 图像校验不通过展示：具体原因 + 拍照指导 + 重新拍照按钮

### 检测页模板 `detect.wxml` & 样式
- 取景框 UI：四角标线 + 居中提示文字
- 拍照后预览
- 图像校验不通过卡片：警告图标 + 原因 + 指导列表 + 重新拍照按钮
- 分析中动画：旋转进度环 + 5步骤列表（完成/进行中/待处理三态）
- 完成页：条带灰度可视化（C/T1/T2 三行进度条 + 数值 + 状态）+ 结果预览卡片 + AI建议 + 操作按钮

### 首页逻辑 `index.js`
- `onShow` 每次刷新数据：用户信息 / 当前成员 / 报告数量
- 快捷操作路由分发：检测(navigateTo) / 报告归档(switchTab) / 趋势追踪(switchTab) / 家庭成员(navigateTo)

### 归档页逻辑 `archives.js`
- 报告按 memberId 筛选加载
- 兼容新旧数据模型（`riskInfo` 补全）
- 三个筛选维度：全部 / 阴性（`riskLevelRaw === '正常'`）/ 阳性（非正常）
- 删除二次确认

### 趋势页逻辑 `trend.js`
- 数据加载 + `hasData` 判定（需 ≥ 2条）
- 趋势摘要生成：比较最新与最早 GCA/AFP 值（±20%判定上升/下降/稳定）
- Canvas 延迟绘制（setTimeout 300ms 确保渲染完成）
- 高清适配：`dpr = wx.getSystemInfoSync().pixelRatio`，Canvas 尺寸 × dpr

### 各页面 .json 配置
- 完善 `index.json`、`trend.json`、`archives.json`、`detect.json` 等页面配置

### 遇到的问题
1. **Canvas 2D 图片加载异步问题**：`canvas.createImage()` 需要 `onload` 回调，与同步流程不兼容 → 全部改为 Promise 封装，并设置独立超时
2. **图像校验误杀问题**：最初校验条件过严，暗带阈值定在 100 导致正常试纸也被判定无效 → 调整为 80，并增加详细校验失败提示
3. **Canvas 尺寸为0**：`initAnalyzerCanvas` 在 `onReady` 中执行，但隐藏 Canvas 的尺寸可能尚未渲染 → 增加 `cw <= 0 || ch <= 0` 防护，返回 Mock 兜底而非崩溃
4. **分析进度动画残留**：页面 `onUnload` 时可能还有未清除的定时器 → 统一用 `_phase1Timer` / `_progressTimer` / `_analysisTimeout` 跟踪，`onUnload` 和 `onAnalysisComplete` 中双重清除
5. **报告页新旧数据兼容**：早期报告数据结构与新阈值引擎输出不一致 → 在 `renderReport` 中做兼容判断，归档页 `loadReports` 中统一补全 `riskInfo`

---

## 第七阶段：在线问诊系统（8月12日）

搭建「患者 ↔ 医生」在线问诊闭环，数据走微信云开发（云数据库 + 云函数）。

### 问诊云函数 `consultation`
- 统一入口 `event.action` 分发：`getOrCreate` / `sendMessage` / `getMessages` / `markRead` / `getPatientList` / `getMyUnread` / `registerDoctor`
- 患者身份按账号 `userId` 隔离，`resolvePatient` 校验账号归属（`users._openid` 与当前 OPENID 一致）
- 医生身份通过 `doctors` 集合登记，`isDoctor` 校验 openid
- 咨询会话写 `consultations`，消息写 `messages`，维护 `doctorUnread` / `patientUnread` 未读计数

### 问诊对话页 `chat`（患者/医生共用）
- `role` 参数区分左右气泡；支持三种消息：文本、图片（上传云存储）、报告分享（`report_share` 结构化 JSON）
- 实时监听 `db.watch` + 3 秒轮询兜底（手机端 watch 可能失效）
- 报告卡片点击：患者端跳本地报告，医生端经 `globalData._sharedReport` 传参展示

### 医生工作台 `doctor`
- 患者咨询列表（`lastMessageTime` 倒序）、未读徽标、隐藏聊天（本地 `doctorHiddenChats`）
- 医生信息卡片（姓名 / 医院 / 科室 / 职称）

### 组件与设置页
- `floating-btn`：全局悬浮问诊按钮（仅患者可见、未读角标、可拖动）
- `report-picker`：报告多选分享弹窗
- `settings`：切换账号（清空数据）/ 退出登录（保留身份）
- `app.json` 注册 `chat` / `doctor` / `settings` 路由，`index` / `archives` / `trend` 注册 `floating-btn`

### 遇到的问题
1. **watch 手机端失效**：`db.watch` 在开发者工具正常、真机可能收不到变更 → 加 3 秒轮询 + `onShow` 刷新兜底

---

## 第八阶段：用户账号体系与问诊云函数化（8月15日）

将「微信一键登录」升级为「用户名 + 密码」账号体系，账号存云数据库、跨设备可登录；问诊数据操作收敛到云函数。

### 用户账号云函数 `user`
- `register`：用户名唯一性校验 + 姓名/用户名/密码注册（密码明文，比赛演示）
- `login`：按用户名+密码匹配，换设备登录时更新 `_openid`（失败不阻塞登录）
- 账号存 `users` 集合，`_openid` 绑定登录设备

### 登录页重构 `login`
- 患者：用户名密码登录 / 注册，成功后写入本地 `userProfile` 实现自动登录
- 医生：固定演示账号（`doctor` / `zhigan2026`）→ 调用 `registerDoctor` 登记 openid
- 换账号登录/注册时清空上一个账号的 `members` / `reports`，避免串号

### 应用入口 `app.js`
- `saveLoginState` 按患者/医生分角色处理；医生角色不创建家庭成员
- 自愈逻辑：旧版医生登录误创建「在线医生」默认成员 → 检测到污染即重置为患者本人

### 问诊数据操作 `utils/consultation.js`
- 全部收敛到 `consultation` 云函数，患者按 `userId` 隔离（不同账号问诊数据互不影响）
- `watch` 实时监听能力保留（需数据库读权限）

### 工具与辅助
- `utils/user.js`：账号注册/登录云函数封装
- `utils/util.js`：新增 `uploadImage` / `getTempFileUrl`（云存储上传与临时链接）
- `proxy/server.js`：本地 Kimi API 代理（开发调试用）
- `README.md` 同步更新目录结构、云开发配置与数据库权限建议

### 遇到的问题
1. **医生登录污染患者档案**：医生角色误走患者初始化流程，创建「在线医生」默认成员 → `saveLoginState` 分角色处理 + 自愈重置
2. **跨账号数据串号**：换账号登录时上一账号的家庭成员/报告残留 → 登录/注册成功时按 userId 差异清空本地数据

---

## 第九阶段：Bug 修复与稳健性加固（8月15日）

针对代码走查发现的 4 个问题做定向修复：

### 在线问诊消息截断
- `consultation` 云函数 `getMessages` 原来 `orderBy('createdAt','asc').limit(100)` 取到的是**最旧 100 条**，对话一旦超过 100 条最新消息就看不到 → 改为 `orderBy('createdAt','desc').limit(100)` 取最近 100 条再 `reverse()` 反转为正序（最新在底部），`hasMore` 置 `false`
- `chat.wxml` 移除失效的「加载更多」按钮，`chat.js` 删除 `hasMore` 字段及赋值

### 无效试纸报告污染归档
- `archives.js loadReports` 增加过滤 `r.isValid !== false`，无效报告不再出现在归档列表与「阳性」筛选中（趋势页此前已做同类过滤，本次对齐）

### AI 异常误报「重度/高危」
- `kimi-proxy` 云函数新增 `toGray()`：灰度字段缺失/非法时返回 `null`，不再用 `|| 0` 兜底成「纯黑=重度」；`isValid` 改为 `result.isValid === true`
- `kimi.js analyzeTestStrip` 新增 `isGray()` 校验，字段非法或 `isValid === false` 时抛错，交由检测页降级到本地 Canvas / Mock

### 医生端错误提示失效
- `doctor.js` 补上 `hasError` / `errorMsg`，`loadPatientList` 失败时设置；`doctor.wxml` 患者列表增加 `!hasError` 条件，错误横幅得以正常显示（此前引用了未定义的 `hasError` 属于死代码）

### 遇到的问题
1. **消息分页误用**：`limit(100)` 配合升序取到的是最旧数据而非最新数据，且「加载更多」无分页参数属于无效交互 → 简化为「始终展示最近 100 条」
2. **灰度兜底值语义冲突**：`Math.round(Number(x)) || 0` 把「缺失」与「合法 0 值（纯黑）」混为一谈，AI 偶发缺字段时被误判为重度 → 缺失返回 `null` 并降级
3. **错误状态未建模**：医生端 wxml 引用了 `hasError` 但 data 中从未定义 → 补齐字段与赋值逻辑

---

## 第十阶段：真机 UI 问题修复（8月15日）

真机调试发现检测页「分析中」进度环的文字与百分比会歪斜（电脑开发者工具正常），做定向修复。

### 检测页进度环文字歪斜
- **现象**：上传图片后进入分析阶段，圆形进度环中央的「智能分析中」字样与百分比在真机上出现倾斜，开发者工具正常
- **原因**：进度环采用「外圈正转 + 内圈 reverse 反转抵消」的双层反向旋转技巧让文字保持不转。电脑上两条 animation 锁帧同步、能正好抵消；真机上两条独立 CSS animation 存在相位漂移，内圈反转无法完全抵消外圈正转，文字随之倾斜
- **修复**：将旋转圆环与文字拆分为两个平级元素——`.ring-wrap` 容器内并列 `.progress-ring`（只负责旋转的圆环，绝对定位铺满）与 `.progress-center`（纯 flex 居中的文字层，不参与任何旋转），彻底解耦文字与旋转
- **涉及文件**：`pages/detect/detect.wxml`（结构调整）、`pages/detect/detect.wxss`（删除 `.progress-bg` 的 reverse 动画，新增 `.ring-wrap` / `.progress-center`）

### 遇到的问题
1. **双层反向旋转在真机不同步**：依赖两条 animation 精确抵消来保持文字不转，真机存在相位漂移 → 改为「旋转元素与文字平级」，文字层不旋转

---

## 第十一阶段：检测原理对标论文重构（8月20日）

围绕那篇 Anal. Methods 2024 论文，把检测原理从「不分方向的灰度→浓度映射」重构为「竞争法 / 夹心法」双向模型——这是本项目最关键的一次原理性修正。

### 检测原理对标（新增「判定规则」目录）
- 新增 `判定规则/` 目录，收录标定依据论文 PDF（《An AgPd NP-based lateral flow immunoassay for simultaneous detection of glycocholic acid and alpha-fetoprotein》，Anal. Methods, 2024）
- 明确两条检测线的免疫学原理**方向相反**：
  - **T1 → GCA（甘胆酸）竞争法**：样品 GCA 越高 → 探针越被样品「抢走」→ T1 线越浅 → 灰度越高
  - **T2 → AFP（甲胎蛋白）夹心法**：样品 AFP 越高 → 结合越多 → T2 线越深 → 灰度越低

### 规则引擎重构 `thresholds.js`
- 重写 GCA / AFP 两张阈值映射表，方向相反（GCA 灰度越高浓度越高，AFP 灰度越低浓度越高），线性插值逻辑通用
- 论文关键数值写入注释：GCA vLOD 50 ng/mL、cut-off 104 ng/mL、正常 0.4–2.98 mg/L；AFP vLOD 0.1 ng/mL、正常 <10 ng/mL、HCC >400 ng/mL
- C 线质控阈值 `C_LINE_VALID_MAX_GRAY` 从 50 调整为 180，避免正常试纸被误判无效

### 本地分析器 `analyzer.js`
- `BAND_REGIONS` 的 T1 / T2 位置对调，匹配论文划线顺序（TL1(GCA) 靠近加样垫、位于最下）
- `sampleRegion` 明确只返回原始灰度、不做浓度转换，方向判定统一收口到 `thresholds.js`

### 云函数 Prompt 更新
- `kimi-proxy` analyze prompt 明确「从上到下依次为 C线(质控)、T2(AFP)、T1(GCA)」及「灰度 0=黑 255=白」

### UI 文案与配色同步
- `detect` / `report` 页条带卡片重命名为 T1(GCA) / T2(AFP)，C 线有效范围文案改为「< 180」
- 条带配色按方向重标：T1 灰度越高越红、T2 灰度越低越红
- 新增提示「GCA（竞争法）：线越浅浓度越高；AFP（夹心法）：线越深浓度越高」

### 登录页视觉
- 新增 logo 图 `images/logo-liver.png`，登录页 logo 区由纯文字升级为图标 + 文字

### 遇到的问题
1. **检测方向性此前被忽略**：旧实现把 GCA 和 AFP 都按「颜色越深越严重」处理，与免疫层析实际原理不符（GCA 竞争法恰相反）→ 对标论文后按竞争法 / 夹心法分别建模
2. **C 线质控阈值过严**：原阈值 50 导致显色稍浅的正常试纸也易被判无效 → 对标实际显色灰度放宽到 180，并同步 detect / report 页文案

---

## 技术总结

| 维度 | 选型 |
|------|------|
| 框架 | 微信小程序原生（WXML + WXSS + JS），无第三方依赖 |
| AI 引擎 | Kimi Vision API（`kimi-k3` 模型），通过 `kimi-proxy` 云函数代理 |
| 图表 | Canvas 2D 自绘折线图 |
| 存储 | `wx.setStorageSync` 本地存储 |
| 分析引擎 | 三层降级：AI → 本地Canvas灰度 → Mock |
| 规则引擎 | `thresholds.js` 灰度分段线性插值 → 四级风险判定 |

### 工程亮点
- **三层降级容错**：确保在任何情况下（网络异常、AI超时、Canvas不可用）用户都能获得分析结果
- **统一规则引擎**：AI / 本地 / Mock 三条路径最终都走同一个 `thresholds.evaluate()`，保证结果一致性
- **图像有效性校验**：本地 10 条横带像素分析，不依赖 AI 就能过滤无效照片，节省 API 调用
- **服务端保管密钥**：Kimi API Key 存放在云函数中，客户端不可见
- **新旧数据兼容**：报告页和归档页兼容旧版数据结构

### 待改进
- GCA/AFP 阈值映射表已完成方向性标定（对标 Anal. Methods 2024 论文），但灰度↔浓度的具体分段仍需结合真实试纸标准数据集微调
- 云开发环境 ID 当前为 `cloud1-d8gjf8tuf7ccf8870`，发布正式环境前需确认/替换
- 可接入微信 `wx.getLogManager()` 实时日志服务，方便线上问题排查
- 趋势图当前仅在 ≥ 2 条数据时绘制，可增加单点展示

---

> 📝 本日志基于文件修改时间戳和代码变更内容整理，由 Claude Code 辅助生成。
